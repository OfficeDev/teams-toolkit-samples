// Defines the settings for the Retail Dashboard application
export interface RetailSettings {
    // Indicates if the application should use mock data or not
    useMockData: boolean;
    // The base URL for the API
    apiBaseUrl: string; 
}
